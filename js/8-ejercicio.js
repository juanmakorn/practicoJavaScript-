/*
un programa que pida un número y diga si es divisible por 2
*/

let numero = parseInt(prompt("ingrese un numero"));
if (numero % 2 == 0 ) {
    document.write("<p>Este numero es divisible en dos</p>");
  } else { document.write("<p>Este numero no es divisible en dos</p>");}

